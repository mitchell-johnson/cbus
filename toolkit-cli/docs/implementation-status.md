# Implementation status and outstanding work

Updated **25 September 2026**. Target: **C-Bus Toolkit 1.18.0.2754 / C-Gate 3.4.0.2001**.

The Python implementation and CLI provide the functions listed below within their stated device, input and backend limits. **Complete Toolkit parity remains unfinished.** The feature ledger contains **38 areas: 17 implemented, 19 in progress and 2 pending**. These are implementation categories, not a percentage of Toolkit functionality. An implemented category can still have outstanding Toolkit or hardware comparisons.

This page covers every area in [capabilities.json](../src/cbus_toolkit/capabilities.json), including completed functions inside unfinished areas. The [README](../README.md) gives command examples, and each linked feature document describes its accepted inputs and tests. The separate [surface census](toolkit-surface.md) inventories all 3,767 indexed help topics and 209 public C-Gate command blocks. Raw command forwarding and the 56 mapped typed-wrapper candidates do not establish acceptance of those workflows; topic and command acceptance in that census remains unassessed.

## Test checkpoints

The last audited installed wheel passed **1,725 tests on Python 3.13.14 and 1,725 on Python 3.10.20** (historical dual-Python checkpoint; current policy is 3.13-only), with no failures, errors or skips and all 14 required native gates enabled. Its exact sources, wheel and reports are recorded in [test-acceptance.json](test-acceptance.json). This remains the latest complete wheel acceptance; the current development tree contains later changes.

Later focused checkpoints below also passed both Python versions, except where explicitly marked 3.13-only under the current single-version policy. Counts include shared regression tests and **must not be added together or added to 1,725**.

| Completed focused function | Tests per Python version | Evidence and limits |
| --- | ---: | --- |
| eDLT configuration CRC | 21 | [CRC](edlt-crc.md); 65,588 fresh original results per run. |
| eDLT percentage arithmetic and CLI input | 10 arithmetic; 9 CLI/helper | [Percentage](edlt-percentage.md); 6,504 original arithmetic rows per run; original lossy roundtrips retained. Full form integration remains open. |
| Retained eDLT scene Trigger invocation | 8 focused | [Scene trigger](edlt-scene-trigger.md); exact retained group/action resolution, one tagged native Trigger request, rejection/uncertainty boundaries and no retries. Physical execution remains unverified. |
| Toolkit About information | 16 | [About](toolkit-about.md); 51 fresh original instruction cases per run; explicit executable and supplied context. |
| Signed update metadata diagnostics | 56 combined | [Metadata](toolkit-update-metadata.md); supplied key remains untrusted. |
| Signed revocation metadata diagnostics | 53 | [Revocation](toolkit-update-revocation.md); historical signer identity, no current-trust or update-availability decision. |
| Supplied-context update conditions | 78 combined | [Registry context v2](toolkit-update-registry-conditions.md); includes the earlier 61-test file-condition/metadata/revocation checkpoint, 11 supported original registry results, 107 captured Int32 comparisons and 248 byte-identical v1 reports. Facts remain unverified. |
| Outgoing CAL route encoding | 24 codec/regression | [Routing](pci-routing.md); separate CLI coverage is included in the 56-test combined checkpoint. |
| Addressed incoming CAL frame inspection | 45 | [Incoming routing](pci-incoming-routing.md); 1,142 fresh original cases per run. |
| Routed RECALL | 73 | [Routed RECALL](pci-routed-recall.md); 428 fresh original matcher cases per run and independent loopback peers. |
| Routed IDENTIFY | 104 | [Routed IDENTIFY](pci-routed-identify.md); 902 original IDENTIFY and 428 RECALL cases per run. |
| Database CSV serializer, selection/encoding preferences, cached projection and native XML adapter | 82 current core/CLI + 2 host Windows guards + 5 owned Windows + 1 owned C-Gate; 23 historical original-inclusive | [Database CSV](toolkit-database-csv.md); current tests add exact 26-column HKCU load/save behavior, Toolkit-native CP_ACP output, the 12-case class/Area/group projection, KEYE1/2/3 ordered associations, per-block secondary-application selection and OID-less native records, DIMDN8/RELDN12 16-slot projection with 8/12 interaction limits, SENPIROA ordered sensor associations, four native archive shapes, live database acquisition, ordered explicit multi-unit and whole-network export from one shared snapshot/request with all-or-nothing rejection before output creation, and guarded B03 Area13 backup/save/reload to the earlier serializer/file boundary. The historical checkpoint performs 88 fresh original comparisons per run; original Toolkit manager enumeration remains unverified. |
| Fourteen thermostat temperature conversions | 17 | [Temperature](thermostat-temperature.md); 28,840 fresh original comparisons per run; separate Windows corroboration. |
| Thermostat scheduling-level retained Python model | 17 | [Scheduling levels](thermostat-schedule-levels.md); replay of all 14 captured original outcomes and their ordered phases. No fresh original or native execution in this earlier core checkpoint. |
| Native thermostat scheduling and CLI | 67 combined | [Native scheduling](native-thermostat-schedule.md); includes the retained core, five native integration methods/eight project-group scenarios, public CLI, backup/save/reload and partial-write/cleanup checks. No fresh original execution or physical devices. |
| Thermostat outer selection (offline CLI, 3.13-only) | 46 combined | [Outer scheduling](thermostat-scheduling.md): 20 model, 9 CLI and 17 shared inner tests. Replays 12 predicate and 12 newly captured original outer workflows, including save order, shared groups, nested locks and partial failures. No native writes or physical devices. |
| Thermostat unit-load and level composition (offline CLI, 3.13-only) | 54 combined | [Scheduling load](thermostat-scheduling.md): includes 5 load-model, 12 scheduling CLI and 37 outer/inner model tests, plus 65 subtests. Replays all 12 captured original AfterLoad outcomes and preserves existing levels through combined missing-level creation. Inputs are supplied raw bytes/resolved groups; no native read/save or physical device. |
| Native thermostat unit-to-level composition (3.13-only) | 14 host; 2 native | [Native scheduling](native-thermostat-schedule.md): 8 manager and 6 CLI host tests plus two fresh owned C-Gate 3.4.0.2001 integrations connect one project/unit XML snapshot to application/group/level planning, one target save, backup and full preservation verification. Zero CNI connections; physical devices remain open. |
| Registry observation wrapper, CLI and host transport | 113 host tests; 7 Windows observations | [Observation API](toolkit-live-registry-observation.md): 78 prior regressions, 30 lazy-wrapper/transport methods and 5 public CLI boundary tests, including retained original vectors, changing repeated reads, exact input hashes, scope/correlation and interruption cleanup. The production worker passed seven typed cases in LocalSystem HKCU with independent key/process cleanup; interactive user-context and original-wrapper comparisons remain open. |
| Rust C-Gate command model and interoperability (Python 3.13) | 13 interop; 58 Rust | [Command coverage](rust-cgate-command-coverage.md): all 224 public manual headings and all 268 bytecode registrations (431 unique paths) dispatch, with dedicated core behavior and deterministic stateful private/application emulation. Production Python transport and wrappers pass against an owned fresh mock; Rust tests, formatting and clippy pass. Hardware effects and process-persistent storage remain explicit boundaries. Earlier [review evidence](../research/experiments/2026-09-24/rust-cgate-review.json) records lower-level corrections. |

Other feature-specific acceptance, including Windows, native C-Gate, transport cleanup and file handling, is linked from the feature documents. Passing simulator tests alone is not evidence of physical-device equivalence.

## Implemented feature areas

“Implemented” below is the ledger status for the stated function. The last column records work that still prevents a broader parity claim.

| Feature area / ledger ID | Implemented and tested functions | Remaining limits or extensions |
| --- | --- | --- |
| Legacy project editing — `legacy-project-editing` | Create, inspect, validate, edit and export XML/CBZ projects; preserve unknown XML, comments, namespaces and attachments; unchanged byte roundtrips and atomic edits. [Project examples](../README.md#legacy-projects). | Independent Toolkit edit comparison; device-specific implicit references; native SQLite editing uses C-Gate separately. |
| Native projects — `native-cgate3-projects` | Create/read/delete, save/reload, copy, archive, restore and rename native projects; ZIP/GZ/DB archives; read-only [repository discovery](repositories.md). Retained [native evidence](native-project-acceptance.json) currently records archive, restore and rename/readback behavior. | Retained native-result evidence for create/read/delete/save/reload/copy; full Toolkit workflow comparison and automatic repository switching. |
| C-Gate transport — `cgate-command-transport` | TCP, verified TLS/mutual TLS, tagged responses, events, here documents, command batches, deadlines, partial errors and first-error preservation. [TLS](native-tls.md), [cleanup](cgate-cleanup.md). | Each command's effects and complete workflows require their own acceptance. |
| Vendor catalogue inventory — `vendor-catalog-inventory` | Extract and enumerate the vendor catalogue, unit profiles and schemas. [Inventory](vendor-inventory.json). | Inventory is not acceptance of every profile or encoding. |
| Documentation surface inventory — `toolkit-surface-census` | Indexed help hierarchy, command blocks, workflow families and implementation candidates. [Census](toolkit-surface.md). | Executable controls, undocumented behavior and independent per-function acceptance remain incomplete. |
| Native unit defaults and database editing — `native-unit-defaults-and-database-editing` | Schema-backed programming sessions, large-memory unit creation, get/set/export/import snapshots, identity checks, saves and interruption cleanup. [Native unit examples](../README.md#c-gate-and-pci). | Missing vendor specifications; uncertain remote resource state after lost replies; physical transfer is separate. |
| Classic key presets — `classic-key-presets` | Eighteen KEY1/KEY2/KEY4 presets with group/block/timer/recall settings and native raw-byte/save/reload checks. [Macros](macros.md). | Other keypad families, arbitrary custom macros and physical button behavior. |
| Neo core presets — `neo-core-key-presets` | Eighteen presets on KEYE1/KEYM4/KEYA3/KEYB4 firmware 2.5.00, including indicators, secondary masks, timers, recalls and scene-bit clearing. [Extended macros](extended-macros.md). | Other firmware/profiles, custom macros and physical buttons. |
| Database addressing — `database-unit-addressing` | Unit moves and closed CNI/Serial network moves, OID/metadata/programming preservation, backups, save/reload and rollback checks; serial matching reports. [Addressing](addressing.md). | Coupled bridge topology and physical moves outside the separate accepted profiles. |
| Native serial retrieval — `native-serial-retrieval` | Cached identity reads, explicit whole-network refresh, duplicate/partial reporting and native decimal-dot normalization. [Serials](serials.md). | Generic cache retrieval does not prove full MMI coverage; broader compatibility. |
| Database serial population — `database-serial-population` | Populate matching KEYE1/KEYGL5/PC_CNIED metadata from a fresh identity inventory, with backup, full XML preservation and save/reload. [Population](serial-population.md). | Other compatibility branches and physical address changes. |
| eDLT Reset controls — `edlt-reset-controls` | Ordered raw Reset, retained model identities and dirty/init phases, plan/stage/discard/save with original and native comparisons. A separate guarded physical FactoryDefault request is documented under the detailed eDLT functions. [Reset](edlt-reset.md). | Unobserved initial contexts, complete form initialization, post-reset physical readback, reboot, persistence and recovery acceptance. |
| eDLT global category programming — `edlt-global-category-programming` | All 16 category masks, source preservation, target backups and full native reload; factory Reset/Project preparation for six captured source patterns. [Global programming](edlt-global-programming.md), [factory CLI](edlt-global-factory-cli.md). | Arbitrary edited UI histories, edits after Reset, missing targets, full form preamble, label transfer and physical programming. |
| eDLT retained scenes — `edlt-retained-scene-editing` | Copy/paste/clear/add/remove, application/trigger/action fields, levels/percent/ramps/synchronization, ordered static scene-name allocation, validation and 64-item retained SceneManager CRC behavior. [Scene manager](edlt-scene-manager.md). | Full SceneManager control binding and physical behavior; partial capacity outcomes cannot be saved. |
| eDLT scene capture/broadcast/trigger — `edlt-scene-live` | One-shot scene capture, verified values into database saves, ordered immediate broadcast, retained binding to native Trigger event, independent fixture persistence and partial-outcome handling. [Live scenes](edlt-scene-live.md), [scene trigger](edlt-scene-trigger.md). | Original asynchronous GUI timing, full form binding, Trigger receipt and physical scene execution. |
| CGL exchange — `cgl-import-export` | Bounded CGL 1.1 import/export wrappers, validation, filtering, backups and skip behavior are implemented and covered by offline tests. | Retained native CGL result evidence, multi-bridge routes and controller differential acceptance. The project-acceptance fixture does not contain a CGL run. |
| eDLT USB diagnostics — `edlt-usb-read-only-diagnostics` | Serial identification/NCC version parsing, package directory metadata, cached USB listing and standard descriptor/string/configuration reads. [Diagnostics](firmware-diagnostics.md), [USB inspection](usb-inspection.md). | Vendor payload compatibility and physical USB hardware acceptance; installation is separate. |

## Areas with completed functions and outstanding work

| Feature area / ledger ID | Functions available and tested now | Outstanding work |
| --- | --- | --- |
| Project repair — `native-project-repair` | Original XML repository repair; bounded portable lexical/XML repair; exclusive output files, native modern-project loading and Windows x86/AMD64 file handling. [Repair](project-repair.md), [Windows evidence](windows-project-repair.md). | SQLite native repair rejects the command; legacy DBVersion 2.2 conversion, non-UTF8/DTD domains, arbitrary corrupted projects and full dialog behavior. |
| PCI transport — `pci-command-transport` | IDENTIFY/RECALL/WRITE/ACK framing and correlation; selected-serial encoding/receipt/one-shot transport; outgoing/incoming route codecs; explicit-path routed RECALL and raw IDENTIFY. [Routing](pci-routing.md), [selected serial](pci-selected-serial.md). | Routed writes, original cached-topology identity, broader asynchronous receivers, full protocol/application coverage and physical routed delivery. |
| Interface setup — `interface-discovery-and-setup` | Discovery and setup command surfaces with simulator/research checks. [Local services](local-native-services.md). | Complete Toolkit interface setup, discovery and real-interface compatibility acceptance. |
| Scan, unravel and routing — `network-scan-unravel-routing` | Native network state/scan controls; duplicate serial evidence; direct all-256-address MMI, serial collection and before/after full inventory; a Rust transport collector that preserves raw duplicate identities and partial observations; exact two-identity selected-serial fixture. [MMI](pci-mmi.md), [inventory](pci-full-inventory.md), [duplicates](duplicate-discovery.md). | General duplicate resolution, full commissioning, multi-bridge topology and hardware compatibility; sequential inventory is not atomic. |
| All unit encodings — `all-unit-parameter-encoding` | 6,487 of 6,497 catalogue boundary configurations compared with C-Gate, 616,722 parameter comparisons and 161 of 163 distinct layouts tested with changing native values. [Catalogue acceptance](catalog-acceptance.md). | Ten vendor-absent boundary specifications native C-Gate itself rejects; two single-bit DLT LabelFlavour layouts with no native token-addressable parameter name; physical transfer/protection. |
| Unit transfer — `unit-read-write-verify` | Database programming; independent raw fixture writes; native KEY4 UnitName read/edit/save/readback over synthetic legacy memory. [Native network](local-native-network.md). | All-device physical read/write/verify, checksum mutation, protection and transfer failures. |
| Copy, convert and reset — `unit-copy-convert-reset` | Native DIN conversion, existing-unit alignment and all nine classic KEY1/KEY2/KEY4 replacement pairs with backup and persistence checks. [Conversion](conversion.md), [classic replacement](classic-replacement.md). | Other keypad/sensor conversions and full reset behavior. |
| Applications and control — `groups-applications-control` | Native lighting ON/OFF/RAMP/STOP, Trigger events and Enable SET; declared KEYE1 nonzero lighting restart recovery. [Enable](enable.md), [Trigger Control](trigger-control.md). | Remaining applications and eDLT-only exact level recovery. |
| Scenes, triggers and labels — `scenes-triggers-labels` | Labels, typed native whole-unit or per-key label-cache clear requests, Trigger Control, scene-file editing/execution/cached recording; KEYE1 40-command stored scenes and key binding. [Scenes](scenes.md), [device scenes](device-scenes.md), [labels](labels.md). | Cache erasure/persistence/readback, native named scenes, other device scenes, learning, physical invocation and persistent scene triggers. |
| DLT/eDLT editors — `dlt-edlt-widgets-and-labels` | The detailed function list below covers widgets, shared settings, retained lifecycle, CRCs, ordered controls, original lossy Measurement Gain/Offset decimal conversion, and database save/reload for the declared eDLT profile. | Full form initialization/bindings, all DLT/firmware profiles, automatic metadata creation and physical display/control behavior. |
| Sensors — `sensors-wizard-semantics` | SENPILL 2.3.00 / 5753PEIRL occupancy events, virtual keys, timers, thresholds, margin and enable dependencies; offline preview and native save/reload. [Sensors](sensors.md). | Other sensor profiles and dialog functions, optical behavior and physical testing. |
| Firmware installation — `firmware-update` | DFU records/descriptors/container checks/plans; original DLL with independent memory; injected and claimed-PyUSB clients for explicit program/erase/readback and partial failures. [DFU protocol](dfu-protocol.md), [transport](dfu-transport.md), [USB DFU](usb-dfu.md). | Vendor firmware payload compatibility, nonzero external-address semantics, NCC updates and physical bootloader/firmware acceptance. |
| Calculator and diagnostics — `network-calculator-diagnostics` | Offline network calculator; native clock target/recovery and burden edits with persisted fixture bytes and per-unit error reporting. [Calculator](calculator.md), [clocks](native-clocks.md). | Electrical arbitration and remaining Toolkit diagnostics. |
| Preferences and updates — `preferences-and-update-workflow` | All 40 preference definitions and five displays; 19 ordered OK-handler edits, retained defaults, Windows registry load/save/reset and address preview; link/catalogue, offline metadata/revocation/condition diagnostics, supplied HKCU registry facts, public bounded live-condition CLI, seven-case production worker acceptance in system-context HKCU and About text. [Preferences](toolkit-preferences.md), [updates](toolkit-updates.md), [conditions](toolkit-update-conditions.md), [registry facts](toolkit-update-registry-conditions.md), [live observation](toolkit-live-registry-observation.md). | Actual VCL behavior/runtime side effects, interactive user-context worker acceptance, original lazy-wrapper comparison, broader condition types, culture-sensitive comparisons, rollout, publisher-chain/current trust, complete applicability and update/install availability. |
| Physical addressing — `physical-unit-addressing` | KEYE1 2.5.00 direct CNI/Serial move to an empty address, explicit serial checks, fresh full MMI, one write, independent verification and recovery journal. [Physical addressing](physical-addressing.md). | Occupied-address displacement, other units/firmware, bridges, exclusion of external controllers and real hardware. |
| Unit templates — `toolkit-unit-templates` | Matching KEY1/KEY2/KEY4 1.2.67 XML template import/export, attribute order/checksum/escaping, previews, identity preservation and native save/reload. [Templates](unit-templates.md). | Other profiles, cross-type conversion, non-ASCII exchange and physical template transfer. |
| Serial commissioning — `serial-directed-commissioning` | Single unaddressed KEYE1 to a unique database match; exact two-identity duplicate fixture with plan/apply/verify; and a bounded cmqttd `NET UNRAVELUNIT ... 255 MATCHDB` backend that moves two known serials once each to unique empty database destinations and verifies complete before/after inventories. [Commissioning](serial-commissioning.md), [direct coordinator](pci-selected-serial.md), [cmqttd service](../../docs/cmqttd-cgate.md). | General duplicates, occupied destinations, cycles, bridges, broader topologies/firmware, real hardware persistence and concurrent-controller exclusion. |
| Database report export — `toolkit-database-report-export` | CSV from explicit captured values, a strict cached-unit/group projection, an admitted native XML file, or one live C-Gate project snapshot. Native XML supports one unit, an explicit ordered multi-unit selection spanning networks, or every unit in one network's document order. Each multi-unit export uses one parsed snapshot and live mode uses one `DBGETXML` request; a missing, ambiguous or unsupported selected unit rejects the entire export before exclusive output creation. The projector reproduces 12 captured original class/Area/group outcomes. Read-only mode covers archived RELAY4 Area12, existing `<Unused>` Area255, generic nil, KEYE1/2/3 firmware 2.5.00 with all nine ordered group associations, optional OIDs and source-pinned per-block secondary-application selection, DIMDN8/RELDN12 firmware 2.7.00 with all 16 associations and their original 8/12 interaction limits, and SENPIROA firmware 2.4.00 with eight ordered sensor associations. Explicit apply mode backs up and completes the exact missing-Area13 fixture with one group creation, save/reload verification and no physical access. The exact original 26-column selection can be loaded/saved through the Windows 32-bit HKCU view. Output supports portable UTF-8 and explicit Toolkit-native Windows ACP conversion. [CSV](toolkit-database-csv.md). | Original Toolkit manager enumeration, secondary-application associations for other families and remaining unit profiles. |
| Thermostat configuration — `thermostat-configuration` | Fourteen scalar conversions; retained inner and outer scheduling models; offline CLI load planning for twelve original programmable-unit outcomes; combined offline load/level creation; native unit XML and six scheduling-byte reads; unit-to-application/group/level preview/apply with backup, exactly one target save, full unit/metadata preservation and reload verification; fresh manager and public CLI execution against owned C-Gate 3.4.0.2001; existing-group level-only CLI with prior native acceptance. [Temperature](thermostat-temperature.md), [scheduling](thermostat-scheduling.md), [native scheduling](native-thermostat-schedule.md). | Inherited original loader and service factories; remaining dialog events and complete settings; native collection-order equivalence and physical behavior. |

## Detailed completed eDLT functions

These functions have implementation and focused evidence for **KEYGL5 / 5055EDL / firmware 5.5.00**, subject to each document's explicit initial-state/cache requirements. Database save/reload does not establish physical unit behavior.

| Function | Implemented scope and evidence |
| --- | --- |
| Lighting widgets | Off/On and Dimmer, placement, shared labels, status, restore levels and ramp settings. [Lighting](edlt.md). |
| Scene widgets and stored scenes | Scene trigger/action references, packing, allocation and CRCs; declarative scene tables. [Scene widget](edlt-scene.md), [scenes](edlt-scenes.md). Retained editing, live capture/broadcast and one-shot [Trigger invocation](edlt-scene-trigger.md) are listed separately above. |
| Enable Control | Fixed application 203, Off/Preset, variable/level and display settings. [Enable](edlt-enable.md). |
| Shutter | Two-key control/presets, shared labels and native save/reload. [Shutter](edlt-shutter.md). |
| Timer | Toggle/Retrigger, target/expiry levels, duration and ramp settings. [Timer](edlt-timer.md). |
| Fan and Multi Level | Speed/level modes, thresholds and shared status strings. [Fan](edlt-fan.md), [Multi Level](edlt-multilevel.md). |
| Room Courtesy | Bell Press/Unused, off/on colours and static/dynamic display references. [Room Courtesy](edlt-room-courtesy.md). |
| Measurement | Device/channel, precision, exact signed mantissa/exponent pairs, original lossy decimal Gain/Offset conversion, prefix/suffix/label and icons. [Measurement](edlt-measurement.md). |
| Time/Date and HVAC display | Standby/functional placement, formats/leading zeroes; application 172 group/zone/precision/units. [Time/Date](edlt-time-date.md), [HVAC](edlt-hvac.md). |
| Multi Room Audio | Zone Control, Source Select, Source Control and shared multiplexer/zone propagation. [MRA](edlt-mra.md). |
| Global display and general settings | Large text, icons, timer flashing, wrap, key timings, reporting, tools access and restore mode. [Display](edlt-display.md), [general](edlt-general.md). |
| Standby, colours and brightness | Enable/duration/page/nightlight, original palettes and control-group dependencies. [Standby](edlt-standby.md), [colours](edlt-colours.md). |
| Navigation | Page modes, nine variants, temperature references and shared page-name storage. [Navigation](edlt-navigation.md). |
| Quick Status | Four modes, palettes, groups and linked threshold control ordering. [Quick Status](edlt-quick-status.md), [Windows comparisons](edlt-quick-status-windows.md). |
| Activation and Page Control | Wake modes, events, activation page/first key; fixed Enable application page group. [Activation](edlt-activation.md), [Page Control](edlt-page-control.md). |
| Clear Dynamic Labels | Guarded one-shot native request, identity/inventory checks and separate receipt/fixture-erasure evidence. [Label clear](edlt-label-clear.md). |
| Live label inventory | One fresh network serial refresh selects exact KEYGL5 5.5.00 records in numeric order for sequential, CRC-checked static configuration reads through `cmqttd`. Physical IDENTIFY4 brackets each memory snapshot; the inventory identity is attached only when both serials match it. Partial identities, per-unit errors and one network-wide transient observation ring remain explicit. [Service scope](../../docs/cmqttd-cgate.md#live-label-reads). |
| Physical FactoryDefault | Guarded one-shot KEYGL5 request through `cmqttd`, exact native OEM control, durable intent, fresh identity/inventory checks, strict ACK correlation, no replay, and separate delivery/readback/persistence evidence. [FactoryDefault](edlt-factory-default.md). |
| Retained model lifecycle and restore editor | Supported load/save phases, supplied cache, all 16 restore controls, display-name coupling and type-reset precedence. [Lifecycle](edlt-lifecycle.md), [restore levels](edlt-restore-levels.md). |
| Ordered application and Corridor controls | Primary/Secondary applications, role exclusions, missing-group handling and timer byte-clamp behavior. [Applications](edlt-applications.md), [Corridor](edlt-corridor.md). |
| Blank selection | Original placement restrictions, unchanged-model identity and restore/global preservation. [Blank](edlt-blank.md). |
| CRC and percentage arithmetic | Original configuration CRCs and exact percentage conversion, including lossy roundtrips. [CRC](edlt-crc.md), [percentage](edlt-percentage.md). |

## Research completed; integration still outstanding

Research captures below explain the remaining work. The scheduling and supplied-registry integrations now have the focused acceptance listed above; these captures do not establish a new complete acceptance run. Authored probe snapshots are under [research/experiments/2026-09-15](../research/experiments/2026-09-15/README.md); proprietary binaries, large raw runs and VM output remain outside Git.

| Research stream | Completed observation | Next concrete work |
| --- | --- | --- |
| Thermostat scheduling | Original inner CreateLevels: 12 cases plus two extra-address cases. Original selection predicates and outer workflow each cover 12 cases. The native API/CLI pass the 67-test dual-Python checkpoint. A further 12-case original AfterLoad capture reaches 71,832 approved instruction entries and now feeds an offline load planner/CLI that composes the outer workflow and level creation while preserving supplied levels. | Connect native reads and the combined application/group/level plan to one accepted native save transaction. Compare native collection order and original service factories; implement remaining settings and verify physical behavior. The failed first adapter and trace-only AfterLoad attempts remain preserved. |
| CSV database projection | The production strict cached-object projector and CLI reproduce all 12 pilot cases: ten completed outcomes and two declared provider stops. Read-only native XML projects three archived relay/generic shapes, KEYE1/2/3 firmware 2.5.00, DIMDN8/RELDN12 firmware 2.7.00 and SENPIROA firmware 2.4.00. It composes admitted units in explicit order across networks or every unit in one network's document order from one shared snapshot/request, with whole-selection rejection before output creation. KEYE `SecondApplicationBlocks` now selects the group cache per block, including same-address primary/secondary identities and the configured secondary tag. Every one of 22 matching KEYE records, four matching DIN records and one matching sensor record in the unchanged GRENACHE snapshot projects with its ordered associations; DIN preserves all 16 slots and applies the original 8/12 interaction limits. Explicit live apply mode completes B03 with a distinct backup, one Area13 group creation, save/reload verification and owned C-Gate acceptance. Eight original backend cases and four isolated native captures passed; the original replay covered all four through 27,661 approved original instruction entries while all 473 historical native inputs remained unchanged. Toolkit-compatible column-selection persistence and native ACP output are implemented and tested on Windows. | Verify original Toolkit manager enumeration; add secondary-application associations for other families, remaining unit profiles and physical behavior. |
| Update registry conditions | The fresh v2 Windows pilot passed 12 original leaves and 12 same-provider witnesses with verified key/handle cleanup. The bounded supplied-fact API and CLI pass 78 tests per Python version, preserving all 248 v1 reports byte for byte. The production observer then passed seven typed LocalSystem HKCU cases with exact provider provenance and independent key/process cleanup. | Repeat the production observer under the interactive user and compare original lazy-wrapper ordering with controlled changes, short-circuiting, unsupported values and interruption. Then compose complete applicability and update-selection workflows; rollout, publisher trust and availability remain open. |
| Percentage and Measurement UI | Standalone original Windows percentage controls: 12 pilot plus 528 full cases, including observed errors and losses. Measurement decimal Gain/Offset conversion now reproduces the retained original invariant vectors and is exposed by both CLI paths alongside exact pairs. | Integrate actual percentage controls with the full editor and compare Windows culture-specific parsing and interactive focus/validation behavior. |

The [development checkpoint](development-checkpoint.md) keeps the earlier failed and draft outcomes separate from the later accepted native scheduling API and CLI.

## Pending acceptance and remaining plan

The broad feature ledger does not replace the remaining executable-level census. Additional documented surfaces still need explicit workflow specifications and implementations: wireless learn/join and gateway mappings; relay/dimmer logic controls across profiles; barcode scanner input and resulting unit selection/creation; project/topology documentation, print and image export; remaining navigation/menu workflows; and controller integration boundaries with external applications such as PICED. These appear in the [census scope and unresolved acceptance](toolkit-surface.md#prioritized-unresolved-acceptance); their presence in help is not a completed Python function.

| Ledger ID | Required work |
| --- | --- |
| `toolkit-differential-acceptance` | Define and compare every remaining Toolkit operation/control, error path and supported device/firmware variation against independent original behavior. Map evidence back to the census. Generic forwarding and tests of our own implementation cannot close this item. |
| `unit-hardware-acceptance` | Verify physical network/device effects, programming, protection, persistence, USB/firmware and recovery on the relevant hardware profiles. Current isolated fixtures do not close this item. |

The immediate implementation sequence is cold database-to-report projection, thermostat unit load/save composition and fresh Windows registry-observer acceptance, followed by broader update-condition providers. Continue the remaining eDLT/form, unit-family, topology, firmware and diagnostic gaps in the tables above. Once the newer accepted changes are integrated, build a fresh wheel and rerun the complete Python 3.13 acceptance with all native gates, retaining failed runs and separate focused evidence.

`cbus-toolkit coverage --require-complete` intentionally returns nonzero while this work remains. No estimated completion percentage is reported because the executable-level functionality census and its acceptance mapping are still incomplete.

Completion-gate note: the coverage predicate once required a `verified` status the ledger never issues (dead since the initial checkpoint); it now requires `census_complete` plus every ledger status `implemented` (`cli.py`). Parity burden stays in the ledger IDs themselves, including the two pending acceptance IDs below, so no acceptance is bypassed. `census_complete` remains false and 21 IDs remain unfinished, hence nonzero. The pinned `test_coverage_cannot_claim_completion` guards the current nonzero behavior.

Disposable-server access: the owned C-Gate configuration defaults to `Program`, which denies the internal `PP LOCK`/`PP NEW` operations. The isolated thermostat unit probe successfully used an explicit `interface 127.0.0.1 Clipsal` grant in its own temporary `access.txt` before starting its own server. It completed seven saved/reloaded parameter cases with all listeners verified as loopback, no CNI connections, and process/storage cleanup confirmed ([retained evidence](../research/experiments/2026-09-24/thermostat-native-access.json)). This removes the earlier claim that an operator login is required for those disposable programming fixtures. Fixture topology and `CBUS_CGATE_SIMULATOR_HOST=127.0.0.1` still need correct provisioning for host-based native tests. The serials discovery timing issue remains open; no test was weakened.

Reproducible-verification recipe (all locally provisionable gates): task-owned Temurin 11.0.32.1+1 JDK (`CBUS_CGATE_JAVA`/`CBUS_CGATE_JAVAC`), vendor dir (`CBUS_LOCAL_CGATE_VENDOR=toolkit-cli/research/vendor/cgate/app`, `cgate.jar` SHA256 `3ec483…`), `PYTHONPATH=src:tests`, host Python 3.13.14. Consolidated current-tree result: full `tests/` **1,838 passed, 222 skipped, 20,458 subtests**, plus `test_native_thermostat_schedule_integration.py` **5 passed**, plus javac-gated originals (`pci_routing`, `pci_incoming`, both routed originals, `project_repair`) **51 passed** — 0 failures; every skip maps to an operator provision (TEST_HOST server, Windows/mono, Toolkit EXE, CSV admission). This is the starting point for the fresh-wheel run, not a substitute for its 14 gates.

The latest Python 3.13 current-tree run passes **2,229 tests**, with **256 provisioning-gated skips** and **18,319 passing subtests**, with no failures or errors; the separate owned C-Gate B03 acceptance also passes ([KEYE secondary-association review](../research/experiments/2026-09-24/csv-keye-secondary-application-review.json)). The earlier source-pinned offline/mock checkpoint remains **1,872 tests**, 239 skips and 17,337 passing subtests ([report](../research/experiments/2026-09-24/offline-test-summary.json)). These are source-tree results, not a fresh installed-wheel or zero-skip acceptance. Later wheel-audit metadata changes have separate focused tests.
## cmqttd hardware-service addition (2026-09-24)

The CLI provides both a selected-device
`cgate edlt-labels //PROJECT/NETWORK/p/UNIT` read and a bounded fresh-network
`cgate edlt-labels --network //PROJECT/NETWORK` inventory through cmqttd's
physical C-Gate service. The network form performs one `NET SYNC` plus
`NET CHECKUNIT` serial refresh, classifies every fresh record and reads exact
KEYGL5 5.5.00 devices in numeric address order. It preserves successful static
configuration reads alongside unsupported firmware, unknown or ambiguous
identities and per-device failures; an incomplete report is returned with a
nonzero exit status.
An address nominated by the fresh wildcard MMI but producing no IDENTIFY4 reply
remains an unknown identity, even when CHECKUNIT describes the observation as
`No units detected`. This prevents a silent physical unit from being excluded
while the all-eDLT inventory is reported complete.

Each selected read obtains live identity and OEM memory without Windows or a
second CNI connection, resolves all 64 static strings and their widget, page and
scene references, and checks a stable header plus the stored text CRC. The CRC
check accounts for Toolkit's zero-padded text projection when physical slots
retain old bytes after the null terminator. Physical IDENTIFY4 reads bracket
each selected memory snapshot. Its fresh inventory identity is attached only
when the before/after physical serials both equal the fresh inventory serial;
mismatches remain per-unit errors without stale identity attachment. Device
reads are sequential, so the result is explicitly non-atomic. The network
command then queries `CMQTT LABELS`
once at network scope and keeps the resulting bounded SAL observations separate
from every device. They are transient, network-wide, recipient-unverified and
always incomplete. Unit-shaped `CMQTT LABELS` requests are compatibility aliases
for that same network ring; they do not prove which display received traffic.
Physical dynamic-label cache readback remains unimplemented and false. See
[service implementation and remaining work](../../docs/cmqttd-cgate.md).

The typed `cgate edlt-widget-groups //PROJECT/NETWORK/p/UNIT` command consumes
the service's separate KEYGL5 static mapping. It requires a successful
whole-network `NET SYNC`, then accepts exactly one matching status-300
`WidgetGroups` getter containing 44 unsigned decimal bytes. The result records
physical synchronized-cache provenance, the exact native CSV, read-only
network-wide sequential observation, and a non-atomic snapshot. It treats the
bytes as opaque and makes no dynamic-label cache, rendering, persistence,
database-update, or direct-CNI claim. Focused module and real CLI transport
tests cover exact ordering, path/envelope matching, every malformed value
class, non-success replies, and no retry. See
[the command contract](edlt-widget-groups.md).

The service separately implements physical `LABEL KFIGET` and `LABEL KFISET`.
Their application token is a native `LabelSupportingApplication` class/scope
gate and is not encoded into KFIGET's fixed selector `0x1c`. The GET operation
performs three volatile parameter-`0xFF` writes before IDENTIFY. Every KFI write
and the GET IDENTIFY request is generation-safe, exact-once and excluded from
transport replay; a lost confirmation faults the programming lane until
reconnect. This remains separate from dynamic-label device-cache readback and
does not complete C-Gate compatibility.

This adds hardware functionality to the separate persistent cmqttd service;
it does not change the mock's compatibility claims or complete the feature census.

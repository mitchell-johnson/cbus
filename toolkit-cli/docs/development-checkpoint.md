# Development checkpoint: 15 September 2026

The initial Git checkpoint is `1b40486` on branch `Toolkit`. The subsequent documentation checkpoint includes the scheduling-core corrections and draft native-adapter regressions written since that commit. Toolkit 1.18 parity remains incomplete; this is a development checkpoint, not a release acceptance claim. See [implementation status](implementation-status.md) for all completed functions and outstanding work.

The last audited complete wheel ran 1,725 tests on both Python 3.13.14 and Python 3.10.20, with no failures, errors or skips. Its exact wheel, inputs and reports are identified in [test-acceptance.json](test-acceptance.json). Later focused checkpoints are documented separately and overlap; their test counts must not be added to that total.

The retained `thermostat_schedule_levels.py` core now has [17 passing tests on each Python version](../research/fixtures/thermostat-schedule-levels-acceptance.json), including all fourteen captured original CreateLevels outcomes and their ordered phases. Review and callback-isolation regressions are complete for this bounded model API. Original research separately covered twelve supplied-provider cases, two additional existing-address cases and twenty-four selection-predicate calls. The focused Python acceptance replays captures; it makes no fresh original, VM or native storage calls. Existing scalar temperature conversions are a separate accepted feature.

The `native_thermostat_schedule.py` adapter remains a draft without CLI integration. Its first actual C-Gate pilot failed after a project reload cleared the session's selected project. Explicit `PROJECT USE` and a regression were added. The owned test project and backup were removed and the empty baseline verified; the failed report is preserved. A corrected fresh native pilot, full persistence/failure acceptance and native collection-order comparison remain outstanding.

The documentation checkpoint reran **29 tests on Python 3.13.14 and 29 on Python 3.10.20**, with zero failures, errors or skips: seventeen retained-core tests plus twelve draft-adapter fake-peer tests. [The development test record](../research/fixtures/thermostat-schedule-development-checkpoint.json) pins the code, tests and reports and identifies the failed native pilot and verified cleanup. These runs enabled no native gates, establish no native adapter acceptance, and overlap the seventeen-test core checkpoint. No new full wheel run is claimed.

CSV database projection and update registry conditions remain research work. The cached CSV getter pilot completed ten normal and two declared provider-stop cases; the next eight-original/four-native backend harness has not executed. Registry pilot v1 failed on a missing forwarding assembly before any semantic case was accepted; cleanup was verified. The fresh v2 dependency/provider-identity proposal has compile/guard checks only and is paused for review. These drafts are not production implementations.

Proprietary Toolkit/C-Gate binaries, installed runtimes, virtual environments, VM job output and large execution archives remain outside Git. Authored experiment source snapshots and their provenance manifest are stored under `research/experiments/2026-09-15`; original execution archives remain at their recorded local paths. Research probes use explicit owned fixtures and are not production entrypoints.

Historical acceptance reports and source archives remain immutable. Documentation updates describe current scope; they do not rewrite the hashes or claim that newer code was tested in an older wheel.

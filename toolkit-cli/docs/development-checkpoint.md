# Development checkpoint

The initial Git checkpoint is `1b40486` on branch `Toolkit`; `fce83df` preserves the retained scheduling corrections and first native-adapter regressions, and `48925ce` records the earlier documentation checkpoint. The September 15 checkpoint added the accepted native scheduling CLI and supplied-registry condition evaluator. The refreshed tree also includes the outer scheduling CLI, a registry observation API, Python 3.13 support policy and a Rust C-Gate mock. **Toolkit 1.18 parity remains incomplete.** See [implementation status](implementation-status.md) for every feature area, completed function and outstanding task.

## Complete wheel acceptance

The latest complete installed-wheel result remains [1,725 tests on each of Python 3.13.14 and 3.10.20](test-acceptance.json) (historical dual-Python checkpoint; current policy is 3.13-only), with zero failures, errors or skips and all 14 native gates enabled. Later focused checkpoints overlap one another and this suite; their counts cannot be added to it. A fresh complete wheel run on Python 3.13 is still required for the newer tree.

## Native thermostat scheduling

The retained core's earlier [17-test checkpoint](../research/fixtures/thermostat-schedule-levels-acceptance.json) compared all 14 captured original CreateLevels outcomes and ordered phases. The subsequent [29-test development record](../research/fixtures/thermostat-schedule-development-checkpoint.json) added twelve fake-peer adapter tests. Those historical reports remain unchanged and made no native persistence claim.

The [native scheduling API and CLI](native-thermostat-schedule.md) now pass **67 tests on each Python version**, with zero failures, errors or skips. This includes the core and fake-peer checks, public CLI and cleanup/output regressions, plus five native integration methods covering eight project/group scenarios per run. Fresh isolated C-Gate processes verified all three actions, original-record preservation, backups, save/reload, second-action no-op, stale metadata rejection and actual partial writes after injected lost Value/TagName receipts. All owned processes and storage were removed; the listening CNI sentinel observed no connections. The [acceptance record](../research/fixtures/thermostat-schedule-native-acceptance.json) pins the sources, 443-file archive and reports.

The first native pilot exposed that project reload clears the command session's selected project; explicit `PROJECT USE` fixed it and the corrected pilot passed 182 commands. Failed fixture and acceptance-preparation attempts remain preserved separately. The focused runs replay the 14 original captures; they perform no fresh original instruction execution. The newer [outer scheduling checkpoint](thermostat-scheduling.md) passes 46 Python 3.13 tests, with 12 fresh original workflow captures now replayed alongside the 12 predicate captures. It validates shared roles, exact save order, nested locks and partial failures.

The separate [original unit-load capture](../research/experiments/2026-09-24/thermostat-unit-load-original.json) then passed twelve frozen `AfterLoadProgrammingInformation` cases through 71,832 approved original instruction entries. It establishes asymmetric flag normalization, derived remote enable, application203 creation, disabled group255 lookup, shared-role reuse, ordered group creation and capacity refusal. The first trace-only harness failure remains preserved. The offline `thermostat-scheduling load` planner replays all twelve outcomes and emits the resolved schema consumed by the scheduling commands. `load-create-levels` preserves existing levels and composes load, outer selection and missing-level creation in one operation; the combined focused suite passes 54 tests and 65 subtests.

The newer `cgate thermostat-schedule-compose` boundary connects one exact project/unit XML snapshot to those retained rules. Its 8 manager and 6 CLI tests cover missing application/group/level creation, shared roles, no-op apply, stale-state rejection, one target save, complete unit/metadata preservation, interruption evidence and public dispatch. Two fresh owned C-Gate 3.4.0.2001 integrations then created 93 levels across three new groups plus application 203, and 31 levels in one shared group through the public CLI; backup, one target save, reload persistence, exact unit preservation and second-apply no-op all passed. The isolated child and storage were removed and its CNI sentinel observed no connection. The inherited loader, original service factories, native collection-order equivalence and physical behavior remain outstanding.

## Supplied registry conditions

[Context v2](toolkit-update-registry-conditions.md) adds explicitly supplied HKCU key/entry existence and bounded string/Int32 content comparisons to the existing command. **78 tests pass on each Python version**, with zero failures, errors or skips: 17 new registry tests and 61 condition/metadata/revocation regressions. These compare 11 supported original registry outcomes, explicitly exclude one culture-sensitive comparison, replay 107 Int32 observations and preserve all 248 pre-change v1 reports byte for byte. The [acceptance record](../research/fixtures/toolkit-update-registry-conditions-acceptance.json) pins the exact inputs and reports.

The original v1 registry pilot failed because an exact forwarding dependency was absent. The fresh v2 pilot passed 12 original leaves and 12 independent same-provider witness reads; all scratch keys and handles were cleaned. Supplied facts remain unverified. The newer [registry observation API and CLI](toolkit-live-registry-observation.md) pass 113 focused host tests on Python 3.13, including the earlier 78 regressions, 30 wrapper/transport tests and five command-boundary tests. The `update-condition-live` command admits three bounded ordinary files, hashes their exact bytes, constructs one finite-scope Windows observer and retains partial evidence through interruption or export failure. A subsequent Windows 11 production-worker run passed seven typed LocalSystem HKCU observations with exact compiler/runtime/provider proof and independent owned-key/process cleanup. Interactive user-context and original lazy-wrapper acceptance, other providers/comparisons, rollout, complete applicability and update availability remain separate work.

## CSV database projection

The production [CSV command](toolkit-database-csv.md) accepts explicit captured report values, a strict cached-unit/group projection, or one admitted native `DBGETXML` Installation snapshot. The cached projector implements all ten normal and two declared provider-stop cases from the getter/class pilot. The read-only native adapter projects archived B01 Area12, B02 existing Area255 and B04 generic nil directly from their unchanged XML. The `cgate database-csv` command acquires the project snapshot with one database read by default and performs no physical access. Its explicit B03 apply mode requires a distinct backup project, creates only application-56 Area13 as `Group 13`, saves, reloads and verifies the complete network before export. The current host CSV set passes 42 tests; a separate owned C-Gate acceptance passes against the pinned vendor service with zero CNI connections and complete cleanup. The separate [eight-case original backend capture](../research/experiments/2026-09-24/csv-original8-analysis.json) and [four native database captures](../research/experiments/2026-09-24/csv-native4-analysis.json) verify literal path/OID reads, unchanged full parameter/XML state, missing-group preservation and owned-server cleanup with no CNI connections.

The [first original replay](../research/experiments/2026-09-24/csv-replay-partial.json) completed B01/B02 and stopped at the first uncovered missing-group method in B03; that failure remains preserved. The [reviewed successor](../research/experiments/2026-09-24/csv-replay-complete.json) captured all four fixtures through 27,661 approved original instruction entries. B01/B02/B04 completed. B03 executed the source-backed application-56 lookup, allocated one group at address 13, assigned `Group 13`, and reached the declared `GroupSave` refusal. The production manager now completes that exact native path with backup and persistence verification. General association construction, arbitrary unit profiles, selection preferences and native Windows encoding remain open.

Authored research source snapshots and their byte hashes are retained under [experiments](../research/experiments/2026-09-15/README.md). Proprietary binaries, large raw reports and VM runtime output remain outside Git. No new complete wheel or physical-device acceptance is claimed by this checkpoint.

## Rust mock and current verification policy

The new `cbus-cgate` model and `cgate-mock` executable pass 50 Rust tests and clippy; 13 Python 3.13 interoperability tests pass. Review corrected PP parameter reply termination, malformed Unicode catalogue handling and queued replies lost on shutdown. [Evidence and remaining mock limits](../research/experiments/2026-09-24/rust-cgate-review.json). These results do not establish native persistence or physical parity.

New wheel audits default to Python 3.13. Explicit dual-version arguments still reproduce the historical 1,725-test summary exactly. The runner, CLI and wheel audit now consistently require a completed census and every ledger feature—including the differential and hardware acceptance features—to be implemented before claiming parity.

The latest broad current-tree run passes **2,168 Python tests** with **255 provisioning-gated skips** and no failures or errors; its invocation is recorded in the missing-Area review. The separately provisioned owned C-Gate B03 test also passes. The earlier source-pinned offline report remains [1,872 tests, 239 skips and 17,337 passing subtests](../research/experiments/2026-09-24/offline-test-summary.json). A newly built wheel separately passes all 46 scheduling tests plus installed entrypoint/metadata checks ([focused wheel result](../research/experiments/2026-09-24/installed-wheel-focused.json)); it is not a replacement for the full 1,725-test historical acceptance. Updated acceptance/audit checks pass 20 tests, and the actual [13-test mock runner](../research/experiments/2026-09-24/mock-focused-acceptance.json) verifies stable explicit binary bytes before and after the run.

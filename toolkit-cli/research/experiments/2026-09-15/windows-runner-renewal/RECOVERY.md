# Verified unchanged Windows runner renewal

Old PID1964 exited gracefully with status0 after exact process/start/ready/user/session identity and an empty ready/admitted queue were checked. Its stopped witness, absent runner count and free exclusive lock were verified before startup. New unchanged runner PID8208 runs under the same original interactive user/SID in session1, with no inherited handles or Job object, and expires at 2026-09-15T10:27:44.0376640Z.

All five one-use compile/capture/stop/inspect/start stages ran exactly once. Actual guest compiler/exit/identity/stop/start files are retained here. UTM return status alone was not considered completion. Prefix, expected old PID and actual ready hash were the only C# helper changes; inverse substitution reproduced both prior sources exactly. The host controller's ROOT constant is explicit because this generation lives on external storage via the runtime symlink. No runner code or four-hour lease contract changed.

The fixed resolver job-probe-50aa12c789494647 completed once and verified current compiler/mscorlib/Framework/OS and runner identity, with identical ready/executable bytes before and after. This was the sole job admitted after startup and it completed before handoff publication. All other Windows owners held submissions. All67 previous-generation manifest files remained byte-identical. The shared full-suite environment was updated only after this proof; previous and updated copies are retained.

For a later renewal, derive a new unique prefix from these exact helpers, replace old PID1964 with the actual current PID8208 and the prior expected ready hash with this generation's ready SHA19de41378e0a061b6871adcae18587fe7598205472b03660ac2eec13eeac0b56, and inverse-check the delta. Follow the prior generation PROCEDURE.md with the new identities; never execute this generation's already admitted scripts again. Inspect actual stopped evidence if the lease has already expired. Do not renew during any accepted run or active/pending job.

This proves owned oracle readiness. It does not claim test-suite success, application parity, physical device acceptance, publisher trust or update availability.

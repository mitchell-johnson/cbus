# Fresh registry pilot v2 proposal — original execution pending review

The first pilot remains failed and unchanged. Its one original invocation returned `System.IO.FileLoadException` for the unlisted `Microsoft.Win32.Registry, Version=5.0.0.0` dependency. Its independent Framework witness returned boxed Int32 1. Cleanup removed the eleven issued case keys, verified the owned root absent, closed all handles, and returned exit 1. The host retained the first error and all 29 unchanged input pins. `../registry-pilot-v1/failure-analysis-v1/analysis.json` SHA-256 `8784a34ea094b12849ee9bf3b941e1c6e5e0c1d615e2db2feb47d9d1d4039103` independently checks the partial runtime/method and cleanup evidence. No semantic condition result was accepted and no job was replayed.

## Exact added dependency

Add only the already-staged vendor file `microsoft.win32.registry.dll`, SHA-256 `e9a9d281c1a708aaae366f82fd6a1742f65da2918cc4fa5eaaaada0be24277d9`, already recorded in the historical SESU `manifest-v2.json`. Its assembly identity is Microsoft.Win32.Registry 5.0.0.0, public key token b03f5f7f11d50a3a. Its sole AssemblyRef is mscorlib 4.0.0.0, public key token b77a5c561934e089. Registry, RegistryKey, RegistryView and ten related types are exported forwarders to that reference. There is no Registry TypeDef or GetValue implementation in this file. Its 63 defined methods belong to resource helpers and compatibility attributes; the initial local inspection incorrectly asserted zero total methods, and that failed assertion/output was preserved. No original method was executed during this inspection.

`../registry-dependency-v1/report.json` SHA-256 `714f780c3ee2babe02dfb70e2a6ef3e1c3157b50ec13592f58e602ae9c9382d3` binds the exact staged bytes, metadata outputs and three original call operands. All three registry leaves call MemberRef `0x0a000054` at their previously pinned IL offsets. No System.Security dependency, guessed download, replacement assembly, binding redirect or provider remapping is proposed.

## Provider identity proof before registry writes

The child loads the one additional hash-pinned forwarding assembly, resolves MemberRef `0x0a000054` directly through the original common module, and requires the resulting MethodInfo to equal the three-argument `Microsoft.Win32.Registry.GetValue` method obtained from the already-pinned Framework mscorlib. It additionally requires the same declaring type and module assembly. This must succeed before any fixture creation or witness call. The separate witness then invokes that exact resolved MethodInfo. It is still a separate observation, not interception of the original call.

The child emits that provider's original MemberRef, actual declaring type/assembly/location/hash, metadata token, signature and bounded raw IL bytes. The host correlates those fields and requires the forwarding assembly in both loaded-assembly inventories. Capturing the CLR method body is metadata observation, not execution. The current API has no explicit RegistryView argument; this remains the process-default provider of the exact x86 process. No x64/view-equivalence claim is made. The facade has no independent registry implementation. Any unexpected provider resolution stops before fixture writes; a newly observed dependency stops the pilot rather than expanding the alias whitelist.

## Fresh ownership and unchanged cases

The new root is `HKEY_CURRENT_USER\Software\CBusCliCondition_d0fe9896d3764e53b7327afb4d1af652`, and guest prefix is `registry-admission-d0fe9896d3764e53`. Compile/original job IDs end `d0fe9896d3764e53`. Output is a new local `pilot-v2` directory. Neither the old root nor old guest artifacts are readopted. The twelve hypotheses, fixture kinds/values, exact leaf methods, 18 method pins, type tokens, SID, x86/invariant culture, operation/cleanup watchdogs, known-only cleanup, output limits, first-error handling and no-retry rules are unchanged. `derive.py` requires the seven sealed v1 source hashes before constructing this draft; it records the fresh identity and source deltas.

The earlier full plan at `../registry-pilot-v1/PLAN.md` (SHA-256 `d32e6c9941a2284ab61ec8809ae9a701eb7bab2782cfc7795c16afc637c5cd04`) supplies the unchanged bounds. The v2 release token is `root-reviewed-registry-twelve-v2`; no original execution is authorized merely by this prepared proposal. Admission would remain one fixed provenance query, one compile job and one original job on unchanged generation PID 8208, expiry 2026-09-15T10:27:44.0376640Z. There is no runner renewal, shared C-Gate/CNI operation, network client or cleanup replay. Source-only absence of network call sites remains distinct from dynamic network enforcement.

Successful observation is still exactly twelve original leaf entries and twelve separate witness reads, ten Boolean results and two expected original errors, all 47 record stages, exact runtime/provider identity and independently successful cleanup. Culture-sensitive ordering and prefix behavior remain outside a general production domain. Registry-v2 production work waits for accepted original observations and a separate domain review.

# Original thermostat selection predicate admission

Prepared only. No original emulation admitted or executed. The first static-only preparation decoded all four complete method ranges and passed source/runtime checks without constructing a Machine. A separate parent review will precede one Python 3.13 process invocation with release `root-reviewed-thermostat-selection12-v1`.

Twelve prewritten cases each invoke RemoteScheduleGroupsSelected and RemoteScheduleLevelsRequired on fresh fixtures (24 entry calls). Original ZoneLevelsExist and IsUnused execute when reached. Explicit providers supply a stable zone-manager receiver, enable Boolean, On/Off/Override group identities, integer group address, and FindLevelByAddress existence. The Find provider accepts only addresses1..31, create=false, and fourth argument0, and returns owned non-null sentinel identities without creating a level. These do not claim original model constructors or provider getters.

Every executed original instruction must match predecoded bytes in the exact four method ranges. Every provider checks both receiver and return-address identity from a decoded original call. Unknown calls, original assertions, writes outside the owned stack, more than100000 instructions or2000 provider events, and incomplete return/stack restoration stop the run. Original image and provider fixture data are read-only to original instructions. No PE entrypoint, native service, UI, socket, registry or hardware operation is reachable. Parent sandbox denies network access independently.

Expected Booleans and ordered provider observations are frozen in cases.json, SHA256 `1d03dccff01026f7071d248711d53aa84400bd943250970d95ee578c2886e616`, before execution. Cases cover enabled/disabled and absent/unused/complete/missing group paths, first/middle/last missing level, and both early-stop and later-group evaluation. This is a finite admission pilot; it does not claim the complete128 combination matrix or changing getter identities.

All original, source, interpreter and relevant package/native library bytes are archived and checked before execution. Inputs use nonblocking/no-follow bounded regular-file descriptors. Child records actual loaded libraries/modules and source hashes before/after, and parent associates them against archived bytes. Each call has2seconds/100000 instructions, whole observed probe20seconds, one child wait45seconds with the inherited reviewed process harness. File-backed output, partial report and first-error evidence are retained, source postchecks run after failure, and no original invocation is retried automatically.

Scope excludes CreateLevels mutation, full CreateRemoteScheduleLevels UI wrapper, button handlers, native cached-load semantics, changed provider facts during an invocation, duplicate object identities, and original exception handling. Those remain separate evidence and implementation tasks.

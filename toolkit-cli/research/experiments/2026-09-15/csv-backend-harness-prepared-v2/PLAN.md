# Held CSV backend corroboration: eight original cases and four native fixtures

Status: source and host-only preparation. No original instruction case, C-Gate process, VM job or hardware action has run for this stage. The earlier serializer88 and cached projection12 evidence remains unchanged. This directory extends that research; it adds no package/CLI API.

The concrete sequence is three explicit invocations: original A01–A08; native capture B01–B04; then network-denied original replay of those same four captured B fixtures. Each stage requires its predecessor's successful report and exact source association where applicable. The four B replay observations are part of the four-fixture corroboration, not a new public projection scope. No launcher automatically reruns or expands a failed case.

## Files and ownership

All sources, inputs, temporary output, archives, child working directories and evidence remain below the task-owned external root `/Volumes/external/cbus-toolkit-research-20260915-document-database`. `ownership.json` identifies `/root/project_store`, token `b0f8078731a54cc4aa069f00b043d210`, APFS volume UUID `24F890EF-A1B9-4B52-8E18-823080A2F0BB`. Outputs must be new, resolved ordinary direct children of `backend-pilot-v1`; a child's `capture` directory must belong to its admitted launcher. File reads use nonblocking/no-follow descriptors, regular-file checks and byte limits. Previous captures are never overwritten or deleted.

- `NativeCSVBackendProbe.py`: unchanged original method execution with bounded cached/RTL/transport providers.
- `cases.json`: eight prewritten A inputs, expected literal command, result, response completion sequence and Area address.
- `run_original.py`: archive-first, file-backed, network-denied A launcher.
- `run_native.py`, `native-cases.json`: fresh owned C-Gate capture and four generated fixture definitions.
- `replay_native.py`, `run_replay.py`: exact native-response/scalar/cache association and separate original B replay.
- `backend_support.py`: host-only ownership, bounded reads, archive verification, first-error and report handling.
- `guard_tests.py`: host-only malformed-input, framing, path, interruption and archive checks; never vendor execution or network/service construction.

Historical extraction utilities, `edit_native.py` (a one-time draft editing script) and `prepare-v1` remain in this directory but are not execution dependencies. `prepare-v2` statically constructs the original instruction map; `original_execution=false`, `preparation_complete=true`, `capture_complete=false` are intentional. It does not construct an emulated machine or execute instructions.

## A: actual methods with a supplied command transport

Toolkit EXE SHA256 `9d01721abab3beb4724511e7d65e39328c0518e0721caa53f4601cded20655ab` and MAP SHA256 `f96f05cef7c2bdf0f295397d97249b50c45db013f3fcaa2c502f76e2c10dd1eb` are mandatory. The inherited cached probe is pinned at `0e3acf18fa5d23a38f3a319aa36af374881ffe880926d195c42d7ce539b32e54`, and its serializer/RTL support remains pinned at `4439ac0052d822fcd912c5235f748ff392871006adfca6ccdb7dc8ff4ec81e52`.

Original CreateFlashObject and GetUnit choose TRELAY4 for the explicitly supplied RELAY4/4.4 registration. The original constructor remains an owned provider. The exact relay agent VMT is `0x124927c`; the source registration graph, rather than a whole startup execution, supplies this agent association. Original StorageLoad `0x7f4160` dispatches actual AgentLoad `0xcb5d0c`, whose QuickGet branch invokes `0xcb87f4` and writes the returned string to unit+0x160. Original Area `0xd23704`, StrToIntDef and group-reference methods subsequently execute. This replaces the earlier supplied StorageLoad value only in this new harness.

The original QuickGet `0xcc335c` receives the actual retained agent, parameter text and stack output pointer. Exact providers replace communicator lookup `0xcac3bc` at caller `0xcc339a`, command construction `0x8435dc` at `0xcc33a8`, command transport `0x843860` at `0xcc3491`, and network path at `0xf2e881`. Constructor class/DL/communicator arguments are checked. Its zeroed owned command has the actual VMT and state2; its optional base result-list field+0x50 is nil. The transcript is owned evidence, not the original collection implementation. No original constructor, native import, PE entrypoint, exception unwinder or timer runs.

The transport provider schedules unchanged GenerateCommandText `0xcb0694` and OnProcessResults `0xcb070c`, with original ECX text and stack argument, on a separate bounded owned stack. Each return checks exact stack balance; final restoration checks EBP/EBX/ESI/EDI and FS. The original QuickGet then copies the result and reaches the exact owned Free call. A whole normal invocation requires its sentinel, exact final ESP and FS=0. Source CFG admission includes normal Delphi pushed finally continuations; exception-dispatch targets remain excluded. Unknown calls or changed bytes stop the process.

| Case | Observation |
| --- | --- |
| A01 | Literal ProjectPrefix+!OID command, terminal Area12 response. |
| A02 | Literal ProjectPrefix+254/p/4 branch for OID text `BLANK`. |
| A03 | Terminal Area255 result. |
| A04 | Ordered 315- continuation then315 terminal; value12 then13, incomplete then complete. |
| A05 | Response named OtherParameter is accepted by the original value parser; this is not verified parameter correlation. |
| A06 | Original missing-parameter408 string completes with an empty result; completion is not successful data retrieval. |
| A07 | Exact hex0xc response through actual AgentLoad and Area/StrToIntDef to existing group12. |
| A08 | Exact decimal12 response through the same actual field/reference path. |

Source correction to the historical proposal: OIDIsBlank is case-sensitive `Pos('BLANK', OID)>0`, not an empty-string predicate. An empty OID would take the OID route and is not silently substituted in A02.

Every machine has an explicit complete owned group cache, source-coded class/field pointers and bounded string/variant/collection providers. One million instructions and three seconds bound each invocation; 60 seconds bounds the finite original batch, with a 75-second file-backed parent timeout and process reaping. Capstone/Unicorn actual loaded libraries must match the previously accepted hashes; Python executable and all declared library sources/binaries are archived and compared before/after. The child additionally records every actually loaded Capstone/Unicorn/pefile Python module path and hash, checks that exact set again, and the launcher associates each with its pre-execution archive. Requested execution, attempted invocations and reached original instruction entries are separate: a support/construction failure cannot set original_execution=true merely because execute mode was requested. The entry counter increments only after the original byte/allowlist hook accepts an instruction address. Preparation may run before review; `execute` requires exact `admission.json` and the archive launch marker.

## B: independent original C-Gate capture

The native driver uses a newly constructed LocalCGate child with original C-Gate3.4.0 build2001 JAR SHA256 `3ec483945102b1355e06163e3ec964797629eb1c5aa50a525f859e5f14ced630`. It never adopts port20033, an existing process, repository or project directory. The explicit Java11 executable, keytool, libjvm, modules image, release file, C-Gate libraries and all unit-specification bytes are captured. Shared package bytes are archived before the first owned socket, associated with loaded modules, and checked again before action and in failure/success finalization.

The child has six verified numeric loopback listeners and fresh Clipsal fixture access. All networks use a separate task-owned loopback sentinel as their CNI description. No NET OPEN, physical parameter operation, repository selector or forwarding command is admitted. Any sentinel connection makes acceptance fail. Setup parameters are written only to generated database projects CSVBA01..CSVBA04.

| Fixture | Native baseline |
| --- | --- |
| B01 | RELAY4/4.4, Area12, all relevant group metadata; also six LogicGA association arrays. |
| B02 | RELAY4/4.4, Area255 with an existing `<Unused>` group. |
| B03 | RELAY4/4.4, Area13 with group13 deliberately absent. PP reads must not create metadata. |
| B04 | OWNED_UNKNOWN/4.4 scalar record; no PP load/spec or QuickGet attempted. |

Concrete setup differs from the historical proposal's PP NEW shorthand: use native PP LOAD of the newly created database unit, RESET_TO_DEFAULTS, six exact SETs, SAVE_TO_SOURCE, PP END and UNLOCK, followed by project SAVE/CLOSE/LOAD. This is the existing supported full-memory allocation path. Manual finite commands avoid automatic library session-recovery requests. SET values preserve required C-Gate escaped spaces. Every PP mutation requires exact status200; generated metadata replies and later complete XML identify the created objects. A setup failure stops without fixture adaptation.

At the read boundary the driver proves a closed idle interface and no retained owned PP session/lock, captures full database XML and all materialized PP values, and requires that the baseline read itself leaves XML unchanged. B01 compares the selected original plaintext RELAY4 parameter definitions with native GET_UNIT_SPEC. Literal original lowercase `pp quickget` commands use both the observed native OID and declared unit path for AreaGroupAddress/Application/GroupAddress, plus B01's six association parameters. Full tagged send/receive bytes and normalized response lines are separate. Greeting is recorded separately before wrapping the socket. A 16MiB wire bound retains an overflowing receive's bounded prefix and byte count; a 20,000-chunk bound is explicit.

Native response validation requires exact continuation/final markers, unique parameter names, requested-name correlation and equality with the full PP baseline. XML, full PP, runtime state, session inventory, metadata address sets and group order are checked again after the reads. No PP session, parameter write, metadata change or PROJECT SAVE occurs within that boundary. Only an explicit final PROJECT CLOSE occurs afterward. Each command has a ten-second deadline, at most512 commands per fixture, and every command also checks the batch's 180-second deadline. Startup and process termination use LocalCGate's separate bounded waits.

The first failed native operation stops the batch and prevents further protocol commands. It closes only owned sockets/process resources, retains partial wire/evidence and performs host-only posthash/report work. There is no retry, recovery save, project rollback or automatic deletion of evidence. Completion requires confirmed child exit/work-directory cleanup and no sentinel connections. Archive writes precede native construction; final reporting independently preserves the first exception object if a later formatting/fsync/close/hash failure occurs.

## B replay and scope

Only after a complete native report and child cleanup does `run_replay.py` launch one separately network-denied original process. It verifies native report/source hashes and archives all exact replay inputs. `replay_native.py` validates every fixed native case, original unit type/firmware/address/OID, the single application and complete ordered group cache, bounded scalar fields, unchanged full PP/XML and literal Area reply. Those native strings initialize explicit cached constructor fields; this is not execution of the cold XML importer.

B01/B02 run the same unchanged actual builder→StorageLoad/AgentLoad→QuickGet parser→Area reference path. B03 reuses the same original group creation path and a newly bounded address-variant provider for exactly13 (the historical fixture admitted255); its GroupSave provider refuses publication. One original in-memory group allocation/address and attempted save must be visible, with a declared partial outcome and no normal Area completion. This is not a successful save or CSV export. B04 uses original generic selection and its nil Area getter; it must make zero QuickGet/storage calls. Native scalars are compared in the constructed field snapshot, and native group tags/OIDs remain explicit references.

Expected B replay count is four captured observations: three normal completions and one declared metadata-save stop. It does not run the CSV serializer or original LoadApplications/LoadDatabaseUnitGroups. A later reviewed stage must compose native XML cold-load/class initialization, application/group/association loading, references and source strings before production database-to-report integration. The accepted offline captured-report serializer remains the only production CSV export boundary.

## Admission and execution handoff

Parent review of final source and PLAN is required; admission files are intentionally absent. After review, A admission is the exact source/cases hash document expected by `run_original.py`, authorization `root-reviewed-csv-backend-original8-v1`. Run A once in a new direct output child with the existing owned Python3.13 environment. Native admission then pins the complete NATIVE_SOURCES hash map and that accepted Stage A report, authorization `root-reviewed-csv-backend-native4-v1`. Run native once with explicit `CBUS_CGATE_JAVA` pointing to the owned Java11 runtime. The B replay is a third explicit invocation on the captured native report, never an automatic fallback or replay of a failed native operation.

No source changes are permitted within an admitted execution. Any new failure remains preserved and returns for review; passing host-only guard tests or static preparation is not original/native acceptance.

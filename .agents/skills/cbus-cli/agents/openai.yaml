interface:
  display_name: "C-Bus CLI"
  short_description: "Use the Toolkit CLI and Rust C-Bus tools"
  default_prompt: "Use $cbus-cli to choose and run the correct C-Bus command-line workflow for this task."

policy:
  allow_implicit_invocation: true
